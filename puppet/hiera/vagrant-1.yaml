# hiera file that is used for Vagrant installs only, never for production

---
classes:
- roles::uber_server

uber::user:                     'vagrant'
uber::group:                    'vagrant'

uber::ssl_port:                 4443
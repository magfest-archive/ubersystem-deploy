---
:backends:
        - yaml
:yaml:
        :datadir: '/usr/local/puppet/hiera/'
:hierarchy:
        - "nodes/secret/%{::fqdn}"
        - "nodes/%{::fqdn}"
        - "nodes/common"
        - "vagrant-%{::is_vagrant}"
        - "common"
:merge_behavior: deeper

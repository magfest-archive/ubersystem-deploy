input {
  lumberjack {
    port => 5678
    ssl_certificate => '/etc/logstashforwarder/ssl/selfsigned-host.crt' # TODO: change this path!! won't exist on clients
    ssl_key => '/etc/logstashforwarder/ssl/selfsigned-host.key'         # TODO: change this path!! won't exist on clients
    codec => "json"

    #add_field => ... # hash (optional), default: {}
    #host => ... # string (optional), default: "0.0.0.0"
    # tags =>
    # type =>
  }
}
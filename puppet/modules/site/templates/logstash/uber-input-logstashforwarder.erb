input {
  lumberjack {
    port => 5678
    ssl_certificate => '<%= @ssl_certificate %>'
    ssl_key => '<%= @ssl_key %>'
    codec => "json"

    #add_field => ... # hash (optional), default: {}
    #host => ... # string (optional), default: "0.0.0.0"
    # tags =>
    # type =>
  }
}
## GENERATED BY PUPPET DO NOT MODIFY

# put this server into hot standby mode
standby_mode = 'on'

# how to connect to the remote PGSQL server
primary_conninfo = 'host=<%= @master_ip %> port=5432 user=<%= @replication_user %> password=<%= @replication_password %> sslmode=require'

# if anyone touches this file, postgres will switch from being the slave
# to being the master.  touch this file if things go bad on the master
# and you need to turn the slave into the master.
trigger_file = '/tmp/postgresql.trigger'

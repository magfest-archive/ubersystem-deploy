# Generated by puppet DO NOT MODIFY

current_theme = "<%= @theme %>"
event_name = "<%= @event_name %>"
organization_name = "<%= @organization_name %>"

show_affiliates_and_extras = "<%= @show_affiliates_and_extras %>"

# TODO: FIX THIS - was renamed
group_reg_available = "<%= @group_reg_available %>"

# TODO: FIX THIS - was renamed
# TODO: these are more like app-specific settings modified after
# the install.  they should be in another config file that
# the user is OK to modify, instead of created by puppet.
group_reg_open = "<%= @group_reg_open %>"

year = "<%= @year %>"

supporter_badge_type_enabled = "<%= @supporter_badge_type_enabled %>"

email_categories_allowed_to_send = <% @email_categories_allowed_to_send.each do |category| -%><%= category %>,<% end -%>

[badge_prices]
initial_attendee = <%= @prereg_price %>
# price_change = boolean(default=False)
# = <%= @at_door_price %>


# hack around a ubersystem+configobj bug.  
# we need to define this here, or else TEMPLATE_DIRS won't read 
# current_theme from parent section.
# see issue https://github.com/EliAndrewC/magfest/issues/189
[django]                                # HACK REMOVE WHEN BUG IS FIXED
current_theme = "<%= @theme %>"         # HACK REMOVE WHEN BUG IS FIXED

[dates]
prereg_opening = "<%= @prereg_opening %>"
prereg_takedown = "<%= @prereg_takedown %>"
uber_takedown = "<%= @uber_takedown %>"
epoch = "<%= @epoch %>"
eschaton = "<%= @eschaton %>"

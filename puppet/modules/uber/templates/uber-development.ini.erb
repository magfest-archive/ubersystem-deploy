# THIS FILE IS GENERATED BY PUPPET
# DO NOT EDIT DIRECTLY, INSTEAD EDIT THE .erb template

path = "/<%= @url_prefix %>"
hostname = "<%= @hostname_to_use %>"

dev_box = <%= @dev_box %>
send_emails = <%= @send_emails %>

# url_root should be the public-facing root of what you type
# to get to this uber instance. do not include the url_prefix/path here.
# 
# if you are behind a proxy like apache, this should be the PUBLIC
# facing address and NOT the internal cherrypy server.
# 
# # example if not behind apache/nginx proxy:
# url_root = "http://public.facing.domain.com:4321"
#
# # example if behind apache/nginx proxy:
# url_root = "http://public.facing.domain.com"
#
url_root = "https://<%= @hostname_to_use %>"

sqlalchemy_url = "postgresql://<%= @db_user %>:<%= @db_pass %>@<%= @db_host %>:<%= @db_port %>/<%= @db_name %>"

aws_access_key = "<%= @aws_access_key %>"
aws_secret_key = "<%= @aws_secret_key %>"

stripe_secret_key = "<%= @stripe_secret_key %>"
stripe_public_key = "<%= @stripe_public_key %>"

# TODO: eventually move stuff below into an event.conf file
# TODO: currently most of the event-specific stuff is in the git
#       repo at magfest/ubersystem and is setup for m13.
#       eventually, remove all the defaults from magfest/ubersystem repo
#       and explictly code them in here.

# at_the_con = "<%= @at_the_con %>"
# max_badge_sales = "<%= @max_badge_sales %>"

[dates]
prereg_open = "<%= @prereg_open %>"
dealer_reg_start = "2014-08-07"
